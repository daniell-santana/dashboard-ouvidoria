server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /dashboard/ouvidoria_dashboard.html;
    }

    location ~ \.json$ {
        root /usr/share/nginx/html;
        add_header Content-Type application/json;
    }
}